echo "# model_corpus_seed_training" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin git@github.com:aaronlumen/model_corpus_seed_training.git
git push -u origin main
